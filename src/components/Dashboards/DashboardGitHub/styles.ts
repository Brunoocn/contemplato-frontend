import styled from "styled-components";

export const Container = styled.main`
  max-width: 1120px;
  margin: 0 auto;
  padding: 2.5rem 1rem;

  h1 {
    color: var(--blue);
    font-weight: bold;
    padding: 1rem 1.5rem;
    text-align: left;
    font-size: 1.4rem;
    line-height: 1.5rem;
    text-transform: uppercase;
  }
`;
